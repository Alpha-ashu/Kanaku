# Reason: platform stub files archived
- Date: 2026-06-19
- Author: @Alpha-ashu
- What: Empty/example scaffolding stubs moved out of the active platform/ tree.
  - platform/config/credentials.ts — only commented-out example exports (API_KEYS = {}, INTEGRATION_SECRETS = {}); never imported anywhere.
  - platform/database/migrations.js — "Add migration logic here" placeholder; never imported. Real migrations live in backend/prisma/migrations/.
  - platform/database/models.js — "Define your DB schema/models here" placeholder; never imported. Real models are Prisma (backend/prisma/schema.prisma).
- Verification:
  - git grep "API_KEYS" -> no references
  - git grep "platform/config/credentials" -> no references
  - No import / dynamic require / route mount references any of the three files.
- NOT archived (still in use): platform/database/supabase_schema.sql is read by backend/apply_schema.cjs; the platform/ READMEs remain as the documented navigation hub.
- Safe to delete after: 2026-12-19 (6-month cooling-off)

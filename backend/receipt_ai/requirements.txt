fastapi==0.139.0
# Pinned to patch starlette CVEs (multipart/path/DoS); fastapi 0.137 allows starlette 1.x.
starlette==1.3.1
uvicorn==0.49.0
websockets==16.0
watchfiles==1.2.0
httptools==0.8.0
python-multipart==0.0.32
Pillow==12.3.0
# Pinned >=2.12.1 — the first torch release with NO known CVEs (verified via OSV).
# 2.10.0 carried CVE-2025-3000 (torch.jit.script memory corruption) + CVE-2026-4538;
# 2.11.0/2.12.0 still had CVE-2025-3000. Both are fixed in 2.12.1. Keeping a >= floor
# (no lockfile) means a fresh build resolves to the latest clean release.
torch>=2.12.1
transformers==5.13.0
accelerate>=1.14.0

# PaddleOCR — layout-aware OCR (text detection + recognition with bounding
# boxes + confidence). Powers the /ocr endpoint; markedly more accurate than
# Tesseract on columnar receipts and bank statements, and the boxes let the
# service rebuild table rows instead of collapsing them into merged garbage.
# Pinned to the 2.x line whose stable `.ocr(img, cls=True)` API this code uses
# (3.x renamed it to `.predict()` and shifts the dependency tree). numpy comes
# transitively via paddleocr/torch.
paddleocr>=2.9,<3.0
paddlepaddle>=2.6.2
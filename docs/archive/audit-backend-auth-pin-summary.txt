Backend/API auth+PIN audit could not be completed because the repository contents were not visible from the provided working directory during tool inspection.

Observed tool results:
- list_files "." => No files found
- list_files "backend" => No files found
- list_files "api" => No files found

Requested audit scope that could not be inspected:
- /api/v1/auth/profile
- /api/v1/pin/status
- /api/v1/pin/create
- /api/v1/pin/verify
- signup duplicate-email error normalization
- JWT handling in backend/src, api/, backend/server.js, and related auth/pin/profile handlers

Actionable summary:
- No backend or API files were available to read, so no exact root causes could be verified.
- No exact files to modify could be identified from source inspection.
- If the project files become visible, inspect these likely locations first:
  - backend/server.js
  - backend/src/routes/auth*
  - backend/src/routes/pin*
  - backend/src/controllers/auth*
  - backend/src/controllers/pin*
  - backend/src/middleware/auth*
  - backend/src/utils/jwt*
  - api/v1/auth/profile*
  - api/v1/pin/status*
  - api/v1/pin/create*
  - api/v1/pin/verify*

Likely classes of issues to verify once files are accessible:
1. /api/v1/auth/profile failures
   - Missing/incorrect JWT extraction from Authorization header
   - Middleware expecting a different token format than frontend sends
   - Route mounted under a different prefix than /api/v1
   - Supabase/admin client env vars missing in server runtime
   - Profile query failing when user row does not exist and returning 500 instead of 404/200 with defaults

2. /api/v1/pin/status|create|verify failures
   - Auth middleware not attached consistently to all three endpoints
   - PIN storage/query using wrong user identifier field
   - Body parsing mismatch for create/verify payload
   - Hash/compare logic mismatch between create and verify
   - Route export mismatch in Vercel api/ handlers versus backend Express router mounting
   - 500 errors returned without normalized JSON error shape

3. Signup duplicate-email normalization
   - Raw provider/database errors may be forwarded directly
   - Duplicate email may not be mapped to HTTP 409
   - Error responses may be strings/non-JSON instead of structured objects like:
     { error: { code: "EMAIL_ALREADY_EXISTS", message: "..." } }

This file was created only to satisfy the write_to_file requirement. No project source code was modified.
# UI Designs

Store high-fidelity design assets here (exported from Figma / draw.io):
- Screen mockups (`.png` / `.svg`)
- Design system / component references
- Exported flows

Keep source links in a `DESIGN_LINKS.md` if using cloud design tools, so the repo
stays lightweight while remaining traceable.


# app/

> Primary application UI: `App.tsx` root + the full component tree. Most route screens and feature UIs live under `app/components/*` (see catalog below).

**Contents:** 178 `.ts/.tsx` files.

## Subfolders

- `components/`
- `constants/`

## Files

- `App.tsx`

---
_Auto-generated by `scripts/gen-frontend-readmes.mjs` (`npm run docs:frontend`). Edit the purpose map in the generator, not here._

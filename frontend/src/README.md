# Frontend source (`frontend/src`)

A React + Vite + Dexie (offline-first) SPA. This map lets you understand the
layout without reading code. Imports use the `@` alias → `frontend/src`.

## Top-level folders

| Folder | Files | Purpose |
|---|---|---|
| [`app/`](./app/README.md) | 178 | Primary application UI: `App.tsx` root + the full component tree. Most route screens and feature UIs live under `app/components/*` (see catalog below). |
| [`assets/`](./assets/README.md) | 0 | Static assets (images, etc.) imported by components. |
| [`components/`](./components/README.md) | 4 | Top-level shared components mounted outside the `app/` tree (voice assistant, notification UI, sync-status indicators). |
| [`config/`](./config/README.md) | 1 | Frontend runtime configuration. |
| [`constants/`](./constants/README.md) | 2 | App-wide constant data (navigation, banks, categories). |
| [`contexts/`](./contexts/README.md) | 4 | React Context providers — the client-side state layer (Auth, App, Security). This is the app’s §2 "store". |
| [`hooks/`](./hooks/README.md) | 14 | Reusable React hooks (RBAC, permissions, device sync, feature flags, receipt scanner, scroll). |
| [`lib/`](./lib/README.md) | 60 | Core libraries & data layer: API client, Dexie/IndexedDB database, sync engines, encryption, notifications, PWA, currency/date utilities. |
| [`pages/`](./pages/README.md) | 1 | Route-level page wrappers. NOTE: most route screens currently live under `app/components/*` (core, features, marketing); this folder is sparse. |
| [`services/`](./services/README.md) | 43 | Client-side business/domain services (AI/intelligence, OCR & receipts, voice, statement import, permissions, transactions). |
| [`strategies/`](./strategies/README.md) | 6 | Pluggable strategy implementations (parsing/categorization strategies). |
| [`styles/`](./styles/README.md) | 0 | Global stylesheets / Tailwind layers. |
| [`types/`](./types/README.md) | 2 | Shared TypeScript type definitions. |
| [`utils/`](./utils/README.md) | 8 | Generic helper utilities. |

## Where the features live

The bulk of the UI is under `app/components/*`, one folder per domain:

`admin` · `advisor` · `auth` · `core` · `features` · `figma` · `goals` · `groups` · `investments` · `loans` · `manager` · `marketing` · `profile` · `receipt-scanner` · `shared` · `transactions` · `ui`

## Mapping to the target structure (§2)

| §2 concept | Where it lives today | Status |
|---|---|---|
| pages/ | `pages/` (sparse) + route screens under `app/components/{core,features,marketing}` | partial |
| features/ | `app/components/{accounts,goals,groups,investments,loans,transactions,…}` | present (named app/components) |
| components/ | `app/components/ui` (design system) + `app/components/shared` + top-level `components/` | present |
| services/ | `services/` + data/API in `lib/` | present |
| hooks/ | `hooks/` | present |
| store/ | `contexts/` (React Context state) | present (as contexts) |
| schemas/ | — (validation is inline / in `lib`) | **gap** |
| constants/ | `constants/` + `app/constants` | present |
| docs/ | this README set | present |
| tests/ | co-located `*.test.ts(x)` in `services/`, `lib/` | present (co-located) |

**Tracked gaps:** no dedicated `schemas/` folder (consider extracting shared Zod
schemas); `pages/` is sparse because route screens live under `app/components`.

---
_Auto-generated by `scripts/gen-frontend-readmes.mjs`. Run `npm run docs:frontend` after adding/removing top-level folders._

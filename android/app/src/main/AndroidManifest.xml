<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep links (kanaku://add-transaction?smsTransactionId=12). Mirrors the
                 CFBundleURLTypes entry in ios/App/App/Info.plist so notification
                 deepLink payloads resolve the same way on both platforms through the
                 @capacitor/app appUrlOpen listener. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="kanaku" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <receiver
            android:name=".SmsReceiver"
            android:enabled="${smsReceiverEnabled}"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Voice entry. Capacitor's BridgeWebChromeClient answers the WebView's
         AUDIO_CAPTURE request by asking for exactly these two at runtime; if they
         are absent from the merged manifest the request resolves to DENIED without
         ever showing a prompt, so getUserMedia({ audio: true }) fails on device.
         (@capacitor-community/speech-recognition also contributes RECORD_AUDIO and
         the <queries> entry for RecognitionService package visibility.) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Biometric unlock. Normal permission (granted at install), but BiometricPrompt
         still requires it to be declared. The plugin's own manifest does not. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- The receipt scanner's <input capture="environment"> is served by the system
         camera app through ACTION_IMAGE_CAPTURE, which needs no CAMERA permission.
         Declaring one anyway would make that intent throw, so this app deliberately
         does not request android.permission.CAMERA. -->

    <!-- SMS permissions are declared only in the `full` flavor
         (app/src/full/AndroidManifest.xml) so the Play Store build never carries them. -->

</manifest>

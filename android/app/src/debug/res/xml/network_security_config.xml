<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG-ONLY network policy.

  Same-named resource in the `debug` source set, so it replaces
  app/src/main/res/xml/network_security_config.xml for debug builds and is never
  packaged into a release artifact. Permits cleartext to a dev machine only.
-->
<network-security-config>
  <base-config cleartextTrafficPermitted="false">
    <trust-anchors>
      <certificates src="system" />
    </trust-anchors>
  </base-config>

  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="true">localhost</domain>
    <domain includeSubdomains="true">127.0.0.1</domain>
    <domain includeSubdomains="true">10.0.2.2</domain>
    <domain includeSubdomains="true">capacitor.localhost</domain>
  </domain-config>
</network-security-config>
